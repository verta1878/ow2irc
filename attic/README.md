# attic/ — working x64 post-processor files

Full working copies with all fixes intact.
DO NOT strip until native x64 CG replaces them.

- x64obj_full.c — 2002 lines, REX pass, branch fixup,
  jump table rewrite, struct patterns, omap, is_branch
- x86enc_with_skip.c — G_UNKNOWN skip for struct ops

## the crew 4free
